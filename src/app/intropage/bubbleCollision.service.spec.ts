import { async, TestBed } from '@angular/core/testing';
import {BubbleCollisionService} from './bubbleCollision.service';


describe('BubbleCollisionService', () => {
  let service: BubbleCollisionService;

  it('can convert speeds from one set of axes to another', () => {
    // expect(component).toBeTruthy();
    // console.log(service.convertToNewAxis(4,5,45));
  });
});
