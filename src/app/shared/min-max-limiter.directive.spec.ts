import { MinMaxLimiterDirective } from './min-max-limiter.directive';
import {Renderer2} from '@angular/core';

describe('MinMaxLimiterDirective', () => {
  it('should create an instance', () => {
    // let elRef = {
    //   nativeElement: document.createElement("input")
    // }
    // let renderer = mock(Renderer2);
    //
    // const directive = new MinMaxLimiterDirective(elRef, Renderer2);
    // expect(directive).toBeTruthy();
  });
});
